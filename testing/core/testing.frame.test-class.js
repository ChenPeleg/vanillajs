class TestObject {
    constructor(descriptopn, test, additionalData) {
        this.descriptopn = descriptopn
        this.test = test
        this.additionalData = additionalData
    }
}

export default TestObject
