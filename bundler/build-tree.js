/**@typedef {{fileContent : string, filePath : string, importPaths : string []}} FileData */
/**type */
export const buildTree = (entryPoint = '../js/main/js') => {
    console.log('bundler');
};

bundler();
